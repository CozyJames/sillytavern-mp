// Copy this file to config.local.js (same folder) and fill in your values.
// config.local.js is gitignored, so `git pull` will never touch it or
// conflict with it.
export const TARGET_URL = 'https://your-server-address:3000';
export const AUTH_TOKEN = 'same-value-as-MP_EXTENSION_TOKEN-on-the-server';
